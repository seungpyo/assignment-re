{
  "name": "my-discord-clone",
  "version": "1.0.0",
  "scripts": {
    "start": "yarn build-client && yarn build-server && yarn server",
    "server": "cd server && yarn start",
    "client": "cd client && yarn start",
    "build-server": "cd server && yarn build",
    "build-client": "cd client && yarn build",
    "quick-upgrade": "cd client && yarn upgrade @seungpyo.hong/netpro-hw && cd .. && cd server && yarn upgrade @seungpyo.hong/netpro-hw && cd .."
  },
  "devDependencies": {
    "@types/uuid": "^9.0.8",
    "concurrently": "^6.5.1",
    "nodemon": "^2.0.22"
  },
  "dependencies": {
    "@craco/craco": "^7.1.0",
    "uuid": "^9.0.1"
  }
}
